Vector: struct {
	x: float;
	y: float;	
};

foo: (a: int, b: float) -> int = a + (int)b;
bar: (a: int) -> int = a;

main: () -> void {
	
	vec: Vector;
	vec.x = 15.5;
	vec.y = 42.0;	
	
	bar((int)vec.x);
	foo((int)vec.x, vec.y);
		
}	

